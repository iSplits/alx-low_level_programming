#include <stdio.h>

/**
 * main - Prints all single digit numbers between of base 10 starting from 0.
 *
 * Return: to 0 means success
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
